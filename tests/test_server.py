# REMOVED — test_server.py tested a FastAPI server.py which no longer
# exists in this repo (removed when migrating from crewAI to OpenCode).
#
# The agent harness is now OpenCode (CLI), not a FastAPI server.
# See README.md for the current architecture.
#
# This file is intentionally left as a placeholder to avoid broken imports.
# A replacement test covering the OpenCode + memory integration is tracked in:
# Future Works #3 — add integration tests for OpenCode agent harness.
